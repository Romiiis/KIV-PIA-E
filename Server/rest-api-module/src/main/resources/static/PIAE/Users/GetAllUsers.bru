meta {
  name: GetAllUsers
  type: http
  seq: 1
}

get {
  url: {{host}}/users
  body: none
  auth: bearer
}

params:query {
  ~role: CUSTOMER
  ~language: cz
}

auth:bearer {
  token: {{bearer}}
}

settings {
  encodeUrl: true
  timeout: 0
}
