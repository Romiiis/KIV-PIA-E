meta {
  name: ApproveProject
  type: http
  seq: 2
}

post {
  url: {{host}}/projects/:id/approve
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{bearer}}
}

settings {
  encodeUrl: true
  timeout: 0
}
