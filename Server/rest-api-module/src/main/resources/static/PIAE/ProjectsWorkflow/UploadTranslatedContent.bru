meta {
  name: UploadTranslatedContent
  type: http
  seq: 1
}

post {
  url: {{host}}/projects/:id/translated
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{bearer}}
}

settings {
  encodeUrl: true
  timeout: 0
}
