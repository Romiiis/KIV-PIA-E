meta {
  name: RejectProject
  type: http
  seq: 3
}

post {
  url: {{host}}/projects/:id/reject
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{bearer}}
}

body:json {
  {
    "text": "This is horrible"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
