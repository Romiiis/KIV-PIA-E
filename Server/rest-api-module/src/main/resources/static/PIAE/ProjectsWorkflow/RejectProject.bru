meta {
  name: RejectProject
  type: http
  seq: 3
}

post {
  url: {{host}}/projects/:id/reject
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "text": "This is horrible"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
