meta {
  name: CloseProject
  type: http
  seq: 4
}

post {
  url: {{host}}/projects/:id/close
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{bearer}}
}

settings {
  encodeUrl: true
  timeout: 0
}
