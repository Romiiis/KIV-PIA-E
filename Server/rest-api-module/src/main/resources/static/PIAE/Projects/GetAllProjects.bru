meta {
  name: GetAllProjects
  type: http
  seq: 2
}

get {
  url: {{host}}/projects
  body: none
  auth: bearer
}

params:query {
  ~languageCode: 
  ~state: CREATED
}

auth:bearer {
  token: {{bearer}}
}

settings {
  encodeUrl: true
  timeout: 0
}
