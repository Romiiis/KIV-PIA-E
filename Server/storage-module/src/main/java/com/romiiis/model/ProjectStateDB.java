package com.romiiis.model;

/**
 * Possible states of a {@link ProjectDB}. Do not change the order!
 */
public enum ProjectStateDB {
    CREATED,
    ASSIGNED,
    COMPLETED,
    APPROVED,
    CLOSED,
}
